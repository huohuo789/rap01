projection;
strict ( 2 );
use draft;

define behavior for ZC_PS01_TRAVEL alias Travel
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

  use action HelloWorld;
  use action CreateTravel;
  use action ApproveTravel;
  use action RejectTravel;
}